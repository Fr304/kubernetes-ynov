# Kustomization pour KubeView
# Usage: kubectl apply -k kubeview/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeview

resources:
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
